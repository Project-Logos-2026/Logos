# Signal Interface (Design Only)

This document names the conceptual signal touchpoints used by Runtime_Orchestration.

It does not define types, schemas, APIs, or implementations.

All authoritative signal semantics are defined in `Signal_Contracts.md`.
