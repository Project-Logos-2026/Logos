{
  "name": "Plan_Object",
  "status": "DESIGN_ONLY",
  "description": "A governed, non-executable representation of intended actions.",
  "required_fields": {
    "plan_id": "string",
    "origin": "string (agent or external)",
    "intent": "string",
    "steps": "list of declarative step descriptors",
    "max_ticks": "integer (>0)",
    "policy_refs": "list of policy identifiers"
  },
  "invariants": [
    "Plan does not execute itself",
    "Plan must be validated before any execution",
    "Plan is immutable once approved"
  ]
}
