{
  "name": "Plan_Tick_Mapping",
  "status": "DESIGN_ONLY",
  "description": "Defines how plan steps would map to tick consumption.",
  "rules": [
    "Each step consumes >= 1 tick",
    "Total ticks <= plan.max_ticks",
    "Budget exhaustion halts execution"
  ],
  "notes": [
    "Execution mapping is deferred to a future phase",
    "This document imposes constraints only"
  ]
}
